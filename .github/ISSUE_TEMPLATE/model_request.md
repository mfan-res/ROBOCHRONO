---
name: Model support request
about: Ask for a model to be added to the roster, or get unstuck adding it
labels: model
---

Most models need no new code — see [docs/ADDING_A_MODEL.md](../../blob/main/docs/ADDING_A_MODEL.md)
first. If you are stuck or the model needs a new adapter, fill this in:

**Model**
- Name and link (Hugging Face card or API documentation):
- Served via API or local weights?
- Architecture / which existing family it resembles, if any:

**What the documentation states** (for the evidence grades)
- Required transformers version (or "the card does not say"):
- Recommended generation settings, if any:
- Thinking/reasoning mode: can it be disabled?

**If you already tried**
- Your configuration JSON:
- Where the verification checklist failed (preflight line, error output):
